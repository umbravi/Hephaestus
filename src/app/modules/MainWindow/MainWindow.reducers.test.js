import { mainReducers } from './MainWindow.reducers'
import actions from './MainWindow.actions'
import { italic } from 'ansi-colors'
import { mainPage } from '../../copy'

describe('MainWindow reducer', () => {
  const initState = {}
  const irrelevantAction = { type: 'IRRELEVANT_ACTION' }

  it('needs reducers', () => {})
})
